# Frontend (placeholder)

This workspace will host the frontend (React/Vite or Next.js). For now this is a placeholder. Recommended next steps:

- Create a Vite React app in `frontend/`:

```bash
cd frontend
npm create vite@latest . -- --template react
npm install
```
