# Food Waste Rescue Network — Backend (MVP scaffold)

Quick start:

1. Copy `.env.example` to `.env` and adjust values.
2. Install deps:

```bash
cd backend
npm install
```

3. Start the server:

```bash
npm run dev
```

Server exposes basic auth endpoints and donation stubs under `/api`.
